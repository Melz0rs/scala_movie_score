package utils

import classes.MoviesDataProviderParseOptions

object MoviesData {
  def parseScore(movieJsonString: String /*json*/, parseOptions: MoviesDataProviderParseOptions): Double = {
    // TODO: Serialize the json string to a dynamic object and extract the correct prop using parseOptions
//    val movieJson = parse
//
//    movieJson[parseOptions.prop]
    5.5
  }
}
